# 软件 叙述段落机械复核

报告：`runs/soft-pool5/gen01/postprocessed/software_apps_zh_意图与聚类叶_跨快照对比.zh.md`　叙述块 7 个，合计 12,553 字符。

**未匹配数字合计 0 个；有问题的引文合计 0 个。**

本检查只拦两件事：叙述里凭空出现的数字、不存在或不可引的例子。它**不**判断数字有没有被用对地方。

**「」只用于引用语料里的真实 query。** 叙述里要强调一个词组时用“”，用「」会被这里当成引文回查。

## 导读
- 数字 106 个，未在表中找到 **0** 个
- 引文 0 个，语料里不存在 **0** 个；存在但命中引用护栏 **0** 个

## 意图层
- 数字 94 个，未在表中找到 **0** 个
- 引文 0 个，语料里不存在 **0** 个；存在但命中引用护栏 **0** 个

## 叶层
- 数字 113 个，未在表中找到 **0** 个
- 引文 0 个，语料里不存在 **0** 个；存在但命中引用护栏 **0** 个

## 子层
- 数字 77 个，未在表中找到 **0** 个
- 引文 2 个，语料里不存在 **0** 个；存在但命中引用护栏 **0** 个

## 独有性
- 数字 66 个，未在表中找到 **0** 个
- 引文 0 个，语料里不存在 **0** 个；存在但命中引用护栏 **0** 个

## 路线交叉
- 数字 70 个，未在表中找到 **0** 个
- 引文 0 个，语料里不存在 **0** 个；存在但命中引用护栏 **0** 个

## 边界
- 数字 85 个，未在表中找到 **0** 个
- 引文 0 个，语料里不存在 **0** 个；存在但命中引用护栏 **0** 个
