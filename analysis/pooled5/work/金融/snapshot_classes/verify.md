# 金融 叙述段落机械复核

报告：`runs/fin-pool5/gen01/postprocessed/finance_zh_意图与聚类叶_跨快照对比.zh.md`　叙述块 7 个，合计 14,742 字符。

**未匹配数字合计 0 个；有问题的引文合计 0 个。**

本检查只拦两件事：叙述里凭空出现的数字、不存在或不可引的例子。它**不**判断数字有没有被用对地方。

**「」只用于引用语料里的真实 query。** 叙述里要强调一个词组时用“”，用「」会被这里当成引文回查。

## 导读
- 数字 160 个，未在表中找到 **0** 个
- 引文 5 个，语料里不存在 **0** 个；存在但命中引用护栏 **0** 个

## 意图层
- 数字 102 个，未在表中找到 **0** 个
- 引文 0 个，语料里不存在 **0** 个；存在但命中引用护栏 **0** 个

## 叶层
- 数字 104 个，未在表中找到 **0** 个
- 引文 5 个，语料里不存在 **0** 个；存在但命中引用护栏 **0** 个

## 子层
- 数字 106 个，未在表中找到 **0** 个
- 引文 7 个，语料里不存在 **0** 个；存在但命中引用护栏 **0** 个

## 独有性
- 数字 67 个，未在表中找到 **0** 个
- 引文 4 个，语料里不存在 **0** 个；存在但命中引用护栏 **0** 个

## 路线交叉
- 数字 93 个，未在表中找到 **0** 个
- 引文 10 个，语料里不存在 **0** 个；存在但命中引用护栏 **0** 个

## 边界
- 数字 121 个，未在表中找到 **0** 个
- 引文 0 个，语料里不存在 **0** 个；存在但命中引用护栏 **0** 个
